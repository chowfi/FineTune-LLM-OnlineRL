# Agent Task Queue

This document tracks prioritized tasks for AI agents. When starting a new session, check this list first.

## Active Tasks (In Progress)
*(Add `[ ]` and move tasks here when an agent starts working on them)*

- `[ ]` (2026-07-03) **MuZero: restart training in latest-selfplay mode.** After merging `muzero-latest-selfplay`, runs default to stock MuZero self-play (`self_play_mode="latest"`); the in-flight frozen-enemy run's checkpoint resumes cleanly into either mode (`--resume checkpoints/muzero_xiangqi/latest.pt` — skips warm start), or restart fresh for a clean latest-mode wandb run. Watch `selfplay/red_win_rate` vs `black_win_rate` (first-mover advantage), `gate/*` as the primary progress signal, and compare draw/entropy/value-cp curves against the frozen-enemy run for the ablation writeup.
- `[ ]` (2026-07-02) **MuZero: first run on the training machine.** On the 5090 box with `PIKAFISH_BIN` set: (1) `PIKAFISH_BIN=... uv run pytest muzero/tests -v` to exercise the 5 engine-gated tests, (2) `uv run python -m muzero.train --smoke --no-wandb --iterations 1 --device cpu` end-to-end smoke, (3) launch the real run `uv run python -m muzero.train`. Watch wandb `selfplay/*`, `loss/*`, `gate/*` and note findings in a dated log.


## Backlog (To Do)
*(Add new ideas, bug fixes, or feature requests here)*

- `[ ]` **MuZero follow-ups from implementation review** (2026-07-02, see `docs/logs/2026-07-02-log-muzero-implementation.md`):
  - Profile `ReplayBuffer.make_target`/`sample_batch` encoding cost under real training (~140 ms/512-batch measured on CPU); vectorize `board_planes` only if it shows up hot.
  - AdamW weight decay currently applies to BN/bias params too; split param groups if runs look off.
  - `gym_xiangqi` perpetual-check ("jiang") can set `done` without flipping the turn; env wrapper credits the mover as winner — verify/fix if self-play logs show it.
  - `run_gate`/warmstart count an engine abort (`if not lines: break`) as a draw / `engine_aborted`; watch gate stats for skew.
  - Gumbel MuZero (32–64 sims) is the designed fallback behind the `mcts.py` interface if 800 sims can't hit throughput on one GPU.
  - The engine-gated self-play smoke test now exercises latest mode (the default); add a frozen-pinned (`self_play_mode="frozen_enemy"`) engine smoke on the training machine before relying on the frozen-enemy ablation for the writeup.
  - Deferred §10 metrics (need extra engine calls / GPU introspection): fraction of moves matching Pikafish best (consider sampling every 10th move); GPU inference batch utilization. (2026-07-04: blunder rate, mate-win rate, and search-gain KL are now implemented — see `docs/logs/2026-07-04-log-training-health-metrics.md`.)
- `[ ]` **Repo hygiene:** 29 legacy `.pyc` files tracked since `6058b0b` (root `__pycache__/`, `scripts/`, `web/server/`, `unsloth_compiled_cache/__pycache__/`) predate `.gitignore` rules; untrack them in a dedicated cleanup commit on main (leave `unsloth_compiled_cache/` contents otherwise untouched).


## Completed
*(Move finished tasks here)*

- `[x]` (2026-07-02) **MuZero for Xiangqi — design + full implementation:** Spec (`docs/superpowers/specs/2026-07-02-muzero-xiangqi-design.md`) and plan (`docs/superpowers/plans/2026-07-02-muzero-xiangqi.md`) executed via subagent-driven development on branch `muzero-xiangqi` — 14 tasks, each with spec + code-quality review. New `muzero/` package (config, encoding, transforms, network, env, mcts, selfplay, replay_buffer, warmstart, train, metrics) with 34 passing tests (5 engine-gated skips locally). Notable review-driven fixes: value support rescaled [-300,300]→[-3,3] h-units, BatchNorm eval() in NetRunner + SimSiam target branch, MCTS MinMax stats tracking child mean-Q, promotion/buffer thread-safety locks, atomic checkpoints. See `docs/logs/2026-07-02-log-muzero-xiangqi-design.md` and `docs/logs/2026-07-02-log-muzero-implementation.md`.
- `[x]` (2026-07-02) **Claude Code migration:** Replaced Cursor agent harness with Claude Code equivalent. Updated `CLAUDE.md` with current project state; created `.claude/settings.json` with a `Stop` hook that enforces handoff accounting. See `docs/logs/2026-07-02-log-claude-code-migration.md`.

- `[x]` (2026-07-01) **Cursor agent handoff harness:** Added an always-applied Cursor rule and project `stop` hook requiring final handoff accounting for `docs/ARCHITECTURE.md`, dated `docs/logs/`, and `docs/AGENT_TODO.md`. Added a repo map to `docs/ARCHITECTURE.md`, expanded `docs/logs/template.md`, and documented the design/plan under `docs/superpowers/`. Hook script verification and scoped Ruff check/format passed. See `docs/logs/2026-07-01-log-cursor-agent-harness.md`.
- `[x]` (2026-05-29) **Self-play enemy Pikafish legality mask:** `generate_self_play_enemy_move()` now applies `apply_pikafish_enemy_legal_mask()` before scoring (matches ally + web UI). New metric `game/enemy_pikafish_prune_rate`. See `docs/logs/2026-05-29-log-self-play-enemy-pikafish-mask.md`. Reviewed eps 5–22 CSV and wandb KL chart. Confirmed all 9 win episodes had `grpo_mean_kl_move=0.0` (pre terminal-win patch); trunc episodes carried bursty updates (ep 20 KL move ~14). Reconciled high KL with learning under `1e-5`: improvements real but updates hot/noisy. Recommended adapter-only resume from `ep_22` with cold optimizer for new `5e-6` / alignment / terminal-win stack. See `docs/logs/2026-05-26-log-long-run-grpo-analysis-and-resume-plan.md`.
- `[x]` (2026-05-22) **Engine-policy auxiliary distillation + smoother GRPO defaults:** Added `grpo/engine_policy_align_coef=0.05`, `grpo/lr=5e-6`, `grpo/ppo_epochs=3`, `grpo/min_batch_reward_std=0.15`; alignment runs even when GRPO PG term is skipped; episode CSV aggregates GRPO over all turns. Not yet validated in a live run (long run eps 5–22 predates this). See `docs/logs/2026-05-22-log-engine-policy-distillation.md`.
- `[x]` (2026-05-25) **Terminal-win GRPO update:** Added a post-`env.step` terminal update path in `LLM_RL_agent_FSDP_v2.py` so an ally move that receives the environment's +100 win reward also gets a terminal-boosted GRPO update on the same sampled candidate group. The raw +100 is mapped to `reward/terminal_win_grpo_reward=10.0` to stay on the dense reward scale while giving the winning move clear advantage signal, especially when Pikafish cannot evaluate the terminal board. See `docs/logs/2026-05-25-log-terminal-win-grpo-update.md`.
- `[x]` (2026-05-21) **Self-play against frozen copy + asymmetric CP-saturation truncation with win-award rewards:** Implemented a robust self-play mechanism using a secondary frozen PEFT adapter ("enemy") that is only updated (via FSDP-safe parameter synchronization) once the learning agent ("default") wins 3 consecutive games. Shifted CP-saturation to be asymmetric (only truncating losing games so the agent gets to practice converting winning endgames) and added a +100 terminal reward to the winning side on truncation to stabilize advantage estimations. Registered `game_consecutive_self_play_wins` in the CSV metrics and ensured seamless resume capability. Documented in `docs/logs/2026-05-21-log-self-play-and-asymmetric-truncation.md`.
- `[x]` (2026-05-17) **Combined `gate + R_best/R_good` reward + optimizer-state resume + curriculum loosening:** `LLM_RL_agent_FSDP_v2.py` now adds a discrete Xiangqi-R1 §3.4 tactical bonus (`reward/tactical_weight * (r_good + r_best)`) on top of the existing `gate` (tanh-of-cp) reward when `reward/combine_gate_with_r_best` is True (default). Per-turn Pikafish `bestmove_root_cached` + best-position cp are precomputed once and shared across the 32 candidates so the extra cost is ~1 Pikafish call/turn. New per-turn and per-episode aggregates (`game/mean_r_best_in_group_rate`, `game/mean_r_good_in_group_rate`, `game/chosen_r_best_rate`, `game/chosen_r_good_rate`); CSV header auto-migrates via `ensure_episode_metrics_csv_schema`. `save_lora_checkpoint` now also writes `optimizer.pt` (Adam `m`/`v` + `global_train_step`) and the resume block in main loads it when `--resume-from` is used, so post-resume Adam moments no longer start cold (fixes the Ep13 first-step `mean_kl_move=3.66` spike). Same patch adds an opponent curriculum (`GreedyEnemyAgent` ε-random override; `enemy/epsilon_start=0.5` linearly decaying to `enemy/epsilon_end=0.0` over `enemy/epsilon_anneal_episodes=25`, anchored at the resumed start-episode) and loosens the truncation curriculum (`game/cp_saturation_threshold` 4000 -> 6000, `consecutive` 3 -> 5) so more games survive long enough for the policy to convert better move selection into actual returns. See `docs/logs/2026-05-17-log-combined-reward-and-optimizer-resume.md`.
- `[x]` (2026-05-20) **Pikafish reliability + gate-only controlled restart:** `pikafish_eval.py` now sets explicit Pikafish `Threads=2` / `Hash=128` and uses a short-lived FEN-level poison cache so critical-error FENs do not cause one restart cascade per candidate move. The next controlled restart returns to the May-16 learning-trend settings: `reward/combine_gate_with_r_best=False`, `game/cp_saturation_threshold=4000`, `game/cp_saturation_consecutive=3`, while keeping the GreedyEnemy epsilon curriculum and May-16 GRPO optimizer settings. See `docs/logs/2026-05-20-log-pikafish-reliability-and-gate-restart.md`.
- `[x]` (2026-05-15) **GRPO optimizer + reward-spread + curriculum tuning:** `LLM_RL_agent_FSDP_v2.py` ships the bundle from the analysis: `grpo/lr` 3e-6 -> 1e-5, `grpo/max_grad_norm` 0.1 -> 0.5, `grpo/ppo_epochs` 2 -> 4, `grpo/beta` 0.05 -> 0.01; `adaptive_cp_scale` cap tightened from 2.0x to 1.2x base; new `grpo/min_batch_reward_std` (0.3) gates `optimizer.step()` on low-std batches with a `[GRPO] Skipping optimizer step:` stdout marker and `grpo/samples_skipped` / `grpo/skipped_low_reward_std` metrics; `game/cp_saturation_threshold` 8000 -> 4000 + `cp_saturation_consecutive` 0 -> 3 truncates hopeless tails so training distribution shifts toward informative positions; new `grpo/grad_norm_pre_clip` diagnostic surfaces whether the new grad-clip cap is binding. See `docs/logs/2026-05-15-log-grpo-optimizer-and-curriculum-tuning.md`.
- `[x]` (2026-05-15) **Resume mechanics + engine-best agreement in CSV:** `LLM_RL_agent_FSDP_v2.py` adds `checkpoint/start_episode`, `--resume-from`, `--start-episode`; new `preload_run_state_from_csv` rebuilds cumulative season/win/lifetime counters from the existing CSV; new `ensure_episode_metrics_csv_schema` migrates the per-episode CSV header in place (with `.bak`) so the seven new `game_engine_best_*` / `game_*chosen_engine_rank*` columns can be appended without losing prior rows. Defaults now point at `checkpoints/xiangqi_grpo_v2/ep_5` with `start_episode=6` and `metrics/clear_csv_on_start=False`. See `docs/logs/2026-05-15-log-resume-from-ep5-engine-best-csv.md`.
- `[x]` (2026-05-14) **Greedy on-policy action selection for legal-move sampler:** `LLM_RL_agent_FSDP_v2.py` now exposes `grpo/legal_move_action_selection` (`"greedy"` default | `"first_sample"`). When greedy, the argmax-policy legal move is placed at position 0 of the GRPO group, so the agent plays its own most-confident legal move while GRPO still trains on the full sampled group. No Pikafish oracle used for action selection. See `docs/logs/2026-05-14-log-greedy-action-selection.md`.
- `[x]` (2026-05-14) **Stdout scoreboard + run heartbeat + hparams:** `LLM_RL_agent_FSDP_v2.py` prints cumulative ally/enemy env returns each episode open and round; default `max_rounds_per_episode=100`, `checkpoint/every_n_episodes=1`; JSON heartbeat (`xiangqi_v2_run_heartbeat.json`, disable with `training/run_heartbeat_path: ""`) for post-mortems (SIGINT/SIGTERM/exceptions/atexit). Documented in `docs/logs/2026-05-14-log-stdout-episode-scoreboard.md` and `docs/ARCHITECTURE.md` §3b.
- `[x]` Set up initial agentic test harness and directory structure.
- `[x]` Establish `docs/logs/template.md` and logging conventions.
- `[x]` (2026-05-11) Legal-move policy sampler, `reward/format_mix_mode=gate`, grounded-move prompt + optional short generate, stricter sampler-path grounding rubric, and related metrics/logging in `LLM_RL_agent_FSDP_v2.py` — documented in `docs/logs/2026-05-11-log-legal-move-sampler.md`.
- `[x]` (2026-05-13) **Rebuild SFT on real PGN + self-play (Xiangqi-R1 §3.1).** Deleted the random-rollout `build_xiangqi_strategy_sft.py` pipeline. New scripts under `scripts/`: `download_xiangqi_pgn.py`, `xiangqi_pgn.py`, `xiangqi_selfplay.py`, `build_xiangqi_sft_dataset.py`, `train_sft_xiangqi.py`, `eval_xiangqi_metrics.py`. Dependency on `cchess` (PyPI). Metrics doc expanded with Online RL signals (`game/mean_ally_cp_after_move_red` + EMA), benchmark bands, and σ constants — see `docs/logs/2026-05-13-log-xiangqi-r1-sft-rebuild.md`. **(2026-05-14)** End-to-end build + train + offline eval numbers recorded in that log §4 and `docs/XIANGQI_R1_METRICS.md` (Recorded offline eval).
